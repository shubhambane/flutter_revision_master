
import 'package:flutter/material.dart';

import 'future/future_home.dart';

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return  MaterialApp(
      home: FutureHome(),
    );
  }
}
